### Navigate the file system

1. Identify a folder on your computer where you (want to) keep course-related files. If you don't have one, choose a suitable location in your file system.
2. Navigate into the chosen location
3. Use the command line to display the path of your chosen directory:


### Create files and folders

1. Within your chosen folder, create a new subfolder and name it `recipes`:
2. Navigate into the `recipes` folder:
3. Create a new file and name it `recipes.txt`
4. Confirm the creation of the file by listing the contents of the `recipes` folder
