2. Use `pwd` to display the path of your current working directory.
3. Use `mkdir` to create a new subfolder and give it a name, within your chosen folder.
4. Use `cd` to navigate into the newly created subfolder.
5. Use `touch` to create a new file and name it appropriately.
6. Use `ls` to confirm that the files was created by listing the contents of the folder.