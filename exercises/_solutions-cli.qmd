[[ slnc 1000 ]]
2. First, we are going to use `pwd` to display the path of the chosen folder.
We want to make sure that we are in the correct location. [[ slnc 1000 ]]
3. Next, we use `mkdir` to create a new subfolder and name it `recipes`, within the chosen folder. [[ slnc 1000 ]]
4. Use `cd recipes` to navigate into the `recipes` subfolder. [[ slnc 1000 ]]
5. Use `touch recipes.txt` to create a new file and name it `recipes.txt`. [[ slnc 1000 ]]
6. Use `ls` to confirm that the file was created by listing the contents of the folder.