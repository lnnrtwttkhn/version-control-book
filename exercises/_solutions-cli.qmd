2. Use `pwd` to display the path of your chosen folder.
3. Use `mkdir` to create a new subfolder and name it `recipes`, within your chosen folder.
4. Use `cd recipes` to navigate into the `recipes` subfolder.
5. Use `touch recipes.txt` to create a new file and name it `recipes.txt`.
6. Use `ls` to confirm that the files was created by listing the contents of the folder.